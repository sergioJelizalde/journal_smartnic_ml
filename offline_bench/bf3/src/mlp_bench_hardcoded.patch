--- mlp_bench.c.orig
+++ mlp_bench.c	2026-07-17
@@ -46,6 +46,12 @@
 #include "mlp_generated.h"
 #endif
 
+#if defined(USE_HARDCODED) || defined(USE_HARDCODED_RAW)
+/* Weight-hardcoded kernel from train_export_hardcoded.py: weights baked into
+ * .rodata pre-packed at generation time, no init call needed. */
+#include "mlp_hardcoded.h"
+#endif
+
 #ifndef WARMUP_ITERS
 #define WARMUP_ITERS 2000
 #endif
@@ -78,7 +84,8 @@
     float *scratch_a = NULL, *scratch_b = NULL, *raw_input, *input;
-#if !defined(USE_XNNPACK) && !defined(USE_GENERATED)
+#if !defined(USE_XNNPACK) && !defined(USE_GENERATED) && \
+    !defined(USE_HARDCODED) && !defined(USE_HARDCODED_RAW)
     posix_memalign((void**)&scratch_a, 16, max_neurons * sizeof(float));
     posix_memalign((void**)&scratch_b, 16, max_neurons * sizeof(float));
 #endif
@@ -87,6 +94,13 @@
     posix_memalign((void**)&input,     16, LAYER_SIZES[0] * sizeof(float));
 
 #if defined(USE_XNNPACK)
     build_xnnpack_runtime();
+#elif defined(USE_HARDCODED) || defined(USE_HARDCODED_RAW)
+    for (int i = 0; i <= NUM_LAYERS; i++)
+        if (HC_LAYER_SIZES[i] != LAYER_SIZES[i]) {
+            fprintf(stderr, "mlp_hardcoded.h shape mismatch vs MODEL_HEADER\n");
+            return 1;
+        }
 #elif defined(USE_GENERATED)
     if (!MLP_GEN_SHAPE_OK()) {
         fprintf(stderr, "mlp_generated.h was generated for a different shape "
@@ -121,6 +129,10 @@
         volatile int cls = predict_mlp_scalar(input, scratch_a, scratch_b);
 #elif defined(USE_NEON)
         volatile int cls = predict_mlp_neon(input, scratch_a, scratch_b);
+#elif defined(USE_HARDCODED)
+        volatile int cls = predict_mlp_hardcoded(input);
+#elif defined(USE_HARDCODED_RAW)
+        volatile int cls = predict_mlp_hardcoded_raw(raw_input);
 #elif defined(USE_AVX)
         volatile int cls = predict_mlp_avx(input, scratch_a, scratch_b);
 #elif defined(USE_XNNPACK)
@@ -129,7 +141,7 @@
         volatile int cls = predict_mlp_generated(input);
 #else
-#error "Define one of USE_SCALAR / USE_NEON / USE_AVX / USE_XNNPACK / USE_GENERATED"
+#error "Define one of USE_SCALAR / USE_NEON / USE_AVX / USE_XNNPACK / USE_GENERATED / USE_HARDCODED / USE_HARDCODED_RAW"
 #endif
         uint64_t t1 = now_ns();
         (void)cls;
